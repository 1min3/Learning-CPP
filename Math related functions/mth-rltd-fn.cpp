// math related functions
#include <iostream>
#include <cmath>

int main()
{

double x = 7;
double y = 4;
double z;
double a;
double b;

z = std::max(x, y);
a = std::min(x, y);
b = pow(2, 2);

//gets the maximum one out of the 2 wla the minimum kima t7ab

std::cout << "the bigger one is " << z << "and the smaller one is " << a << std::endl;
std::cout << "the power of 2 is " << b;

}